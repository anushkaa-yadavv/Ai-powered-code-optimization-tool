[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-code-engine"
version = "4.0.0"
dependencies = [
    "groq",
    "python-dotenv",
    "flask"
]

[project.scripts]
aicode = "ai_code_engine.cli:cli"